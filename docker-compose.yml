services:
  ps01-support-agent:
    build: .
    container_name: ps01-support-agent
    environment:
      - OPENAI_API_KEY=your-openai-api-key-here
    stdin_open: true
    ports:
      - "5000"
    tty: true
    networks:
      - agents-net

networks:
  agents-net:
    external: true